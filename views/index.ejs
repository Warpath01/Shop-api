<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <header>
        <h2><a href="/" style="text-decoration: none;">SHOP-APi</a></h2>
        <nav>
            <% if (user ){ %>
                <a href="/orders">My Orders</a>
                <form action="/auth/logout" method="post">
                    <button>Log Out</button>
                </form>
                <%} else { %>
                    <u>You may</u>
                    <a href="/login">Log in</a>
                    <u>OR</u>
                    <a href="/signup">Sign up</a>
                    <u>an account.</u>
                    <% } %>
        </nav>
    </header>
    <% if (success && success.length> 0){ %>
        <p id="success-message"
            style="border: 2px solid green; background-color: #e6ffed; padding: .6rem; border-radius: 5px; color: green;">
            <%= success[0] %>
        </p>
        <script>
            setTimeout(() => {
                const msg = document.getElementById('success-message');
                if (msg) {
                    msg.style.display = 'none';
                }
            }, 3000); // 3000ms = 3 seconds
        </script>
        <% } %>
            <main>
                <% if (user ){ %>
                    <h3>Welcome (<%= user.fullname %>)
                    </h3>
                    <p>- You can order anything here.. -> <a href="/products">Browse from Shop</a></p>
                    <%}%>

            </main>
            <footer>
                <small>&copy; <%= new Date().getFullYear() %> SHOP-APi</small>
            </footer>
</body>

</html>